name: check
 
on:
  issues:
    types: [opened]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@main
      - name: Run Action
        uses: ./
        with:
          token: ${{ github.token }} # default token in GitHub Workflow
          author: anushkrishnav
          repo: anushkrishnav/Issue-Police
